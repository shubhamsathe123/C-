#include<iostream>
using namespace std;
int main()
{
 int no,temp,rev,sum=0;
 cout<<"enter the no:\n";
 cin>>no;
temp=no;
while(no>0)
{
 rev=no%10;
 sum=sum+(rev*rev*rev);
 no=no/10;
}
if (temp==no)
{
 cout<<"no is armstrong:\n";
}
else
{
 cout<<"no is armstrong:\n";
}
}
